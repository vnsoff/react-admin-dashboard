import { Box } from "@mui/material";
import Header from "../../components/Header";

const FAQ = () => {
  return ( <Box m="20px">
    <Box display="flex" justifyContent="space-between" alignItems="center">
    <Header title="Bar Charts" subtitle="Rectangular bars with heights or lengths proportional to the values."/>
    </Box>
  </Box>  
  );
};

export default FAQ;